
NODE endpoint
SQL >
    %
    SELECT
        country,
        city,
        browser,
        os,
        device
    FROM
        pm_click_events
    WHERE
        view_id = {{ String(viewId, required=True) }}
    LIMIT 1
