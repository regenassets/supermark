// Permissions stubs - advanced UI removed (basic permissions still work)
export const DataroomLinkSheet = ({ children }: any) => children;
export const PermissionsSheet = ({ children }: any) => children;
